.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions


yarn.build-error.log

# Swap the comments on the following lines if you don't wish to use zero-installs
# Documentation here: https://yarnpkg.com/features/zero-installs
# !.yarn/cache # removed cache, to not clutter the repository but still have the pnp feature
#.pnp.*

.idea
.cache

perf-test
perf-logs

lib
lib-bin
dist
*.zip
